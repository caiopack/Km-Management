spring.datasource.url=jdbc:mysql://localhost:3306/km_management?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=root
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

api.security.token.secret=chave-super-secreta

# --- SEGURANÇA DE REGISTRO (DEFINIÇÃO DE CARGOS) ---
# Token para criar funcionário comum (USER)
api.security.token.user=KART2025
# Token para criar gestor (ADMIN)
api.security.token.admin=KART_MASTER_KEY